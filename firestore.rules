rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Authorization is enforced by the application code
    // Read: public (for count queries via REST API)
    // Write: authenticated only
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
